import { makeStyles, tokens } from "@fluentui/react-components";

export const useCreateCommentFormStyles = makeStyles({
  field: {
    width: "100%",
  },
  submit: {
    marginTop: tokens.spacingVerticalM,
  },
});
